#!/bin/sh
# Internet module. Show if internet is up or not
INTERVAL=60

while true; do
	find "/sys/class/net/" -name "operstate" | grep -q '.' || echo "I❌"
	case $(cat /sys/class/net/w*/operstate) in
		down) echo "I❌" ;;
		up) echo "I📶" ;;
		*) ;;
	esac
	sleep $INTERVAL
done
