#!/bin/sh
# Battery module. Show battery left
INTERVAL=30

while true; do
	ACPI=$(acpitool -b)
	case "$ACPI" in
		*Discharging*) ICON=🔋 ;;
		*) ICON=🔌 ;;
	esac
	echo "B$ICON $(echo $ACPI | grep -Po '[\.0-9]+%')"
	sleep $INTERVAL
done
