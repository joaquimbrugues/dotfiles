#!/bin/sh
# Coins module. Show exchange rate for certain coins (LBC)
INTERVAL=7200

while true; do
	ping -q -c 1 1.1.1.1 > /dev/null || exit 0
	RATE="$(curl -s https://eur.rate.sx/1lbc)"
	printf "Y%.2f€" "$RATE"
	sleep $INTERVAL
done
