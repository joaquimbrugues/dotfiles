#!/bin/sh
# Wrapper for passmenu with the system colors

. $HOME/.local/share/global-style

passmenu -nb "$COLOR_BACKGROUND" -nf "$COLOR_FOREGROUND" -sb "$COLOR_UNDERLINE" -sf "$COLOR_FOREGROUND_SELECTED" -fn "$BAR_FONT" "$@"
