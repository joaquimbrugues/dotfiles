#!/bin/sh
# Set the specified wallpaper

# Load the global configuration
[ ! -h $HOME/.local/share/global-style ] && ln -s -T $HOME/.local/share/global-style-blue $HOME/.local/share/global-style
. $HOME/.local/share/global-style

feh --bg-scale "$WALLPAPER"
